class labrary
    def __init__(self,list,name);
    self,booklist = list
    self.name = name
    self.lendDict = {}

    def displaybooks(self):
     print(f"we have following books in libary:(self.name}")
     for book in self.bookslist:
         printf(book)
    def lendbook(self,user,book):
        if book not in self({book:user})
